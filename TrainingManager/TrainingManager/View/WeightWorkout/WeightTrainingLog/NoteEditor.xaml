<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TrainingManager.View.Styles"
             x:Class="TrainingManager.View.WeightWorkout.WeightWorkoutNotes.NoteEditor"
             Title="Comment">
    <ContentView.Resources>
        <local:Style/>
    </ContentView.Resources>
    <StackLayout>
        <Label Text="{Binding ExerciseName}" Style="{StaticResource Title}"/>
        <Grid VerticalOptions="FillAndExpand">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <ScrollView Grid.Row="0" VerticalOptions="Fill">
                <Editor  Text="{Binding ExercieNote}" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" Margin="15,0"/>
            </ScrollView>
            <Button Grid.Row="1" Text="Save" Command="{Binding CalculateMaximumCommand}" Style="{StaticResource RedButton}"/>
        </Grid>
    </StackLayout>
</ContentPage>