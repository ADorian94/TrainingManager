<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TrainingManager.View.Styles"
             xmlns:controls="clr-namespace:TrainingManager.View.Controls"
             x:Class="TrainingManager.View.Timer.IntervallTimer.IntervallWorkoutsPage" >
    <ContentView.Resources>
        <local:Style/>
    </ContentView.Resources>
    <StackLayout>
        <!--<Label Text="" Style="{StaticResource Title}"/>-->
        <ListView x:Name="WorkoutList" ItemsSource="{Binding AllIntervallWorkouts}" SeparatorVisibility="None" HasUnevenRows="True" SelectedItem="{Binding SelectedWorkout}" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <controls:LowSimpleCard CardMainText="{Binding WorkoutName}" ButtonCommand="{Binding BindingContext.CardButtonsCommand, Source={x:Reference WorkoutList}}" ButtonCommandParameter="{Binding WorkoutIdString}"/>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Text="New" Command="{Binding OpenNewIntervallWorkoutCommand}" Style="{StaticResource BlueButton}"/>
    </StackLayout>
</ContentPage>