<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TrainingManager.View.Styles"
             x:Class="TrainingManager.View.SettingsPage"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="{StaticResource AppBackgroundColor}">
    <ContentPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <local:Style/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Settings" Style="{StaticResource Title0}" VerticalOptions="StartAndExpand"/>
            <Grid RowDefinitions="auto, auto" VerticalOptions="End">
                <Button Grid.Row="0" Style="{StaticResource RedButton}" Text="Upload image" Command="{Binding UploadImageCommand}"/>
                <Button Grid.Row="1" Style="{StaticResource BlueButton}" Text="Sign out" Command="{Binding SignOutCommand}"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>